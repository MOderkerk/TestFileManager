language: java
jdk: 
  - oraclejdk8
  - openjdk8
branches:
  only:
  - master
script:  
  - mvn install -DskipTests=false -Dmaven.javadoc.skip=true -Pdeploy  -B 
cache:
  directories:
  - $HOME/.m2
